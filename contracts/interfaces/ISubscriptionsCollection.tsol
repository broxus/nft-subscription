pragma ever-solidity >= 0.62.0;

interface ISubscriptionsCollection {
    event ManagerAdded(address newManager);
    event ManagersReset();

    function mintNft(address _recipient, uint64 _expireAt) external;
}
